name = "edgo-frontend" # 원하는 프로젝트 이름으로 변경 가능
type = "pages"
account_id = "" # 이 필드는 Cloudflare 대시보드에서 가져와야 하지만, CI/CD 환경에서는 환경 변수를 사용하므로 일단 비워둘 수 있습니다.
pages_build_output_dir = "./dist"

[site]
# 빌드된 정적 파일이 있는 디렉토리를 지정합니다.
bucket = "./dist"